
  # Rediseño sitio web iProcess Ind (copia)

  This is a code bundle for Rediseño sitio web iProcess Ind (copia). The original project is available at https://www.figma.com/design/VBpcLlSivwGcQBvcvsMCPH/Redise%C3%B1o-sitio-web-iProcess-Ind--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  